#include <bits/stdc++.h>
#include<iomanip>
#include<numeric>
using namespace std;
bool isprime(int x){
  if(x==2)return true;
  else{
    for (int i = 2; i < x; i++)
    {
     if(x%i==0)return false;

    }
    
  }
  return true;
}
 void solve(){
int rate;
cin>>rate;
if(rate<=1399)cout<<"Division 4";
else if(rate>=1400 && rate<=1599)cout<<"Division 3";
else if(rate>=1600 && rate<=1899)cout<<"Division 2";
else if(rate>=1900)cout<<"Division 1";
 }
int main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int t;
cin >> t;
while(t--){
    solve();
cout<<endl;}

}
